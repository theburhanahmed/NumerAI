# Django Settings
DEBUG=False
SECRET_KEY=<GENERATE_ON_RENDER>
DJANGO_SETTINGS_MODULE=numerai.settings.production
ALLOWED_HOSTS=numerai-backend.onrender.com

# Database (Auto-configured by Render)
DATABASE_URL=<AUTO_FROM_RENDER_POSTGRES>

# Redis (Auto-configured by Render)
REDIS_URL=<AUTO_FROM_RENDER_REDIS>

# Celery (Auto-configured by Render)
CELERY_BROKER_URL=<AUTO_FROM_RENDER_REDIS>
CELERY_RESULT_BACKEND=<AUTO_FROM_RENDER_REDIS>

# CORS
CORS_ALLOWED_ORIGINS=https://numerai-frontend.onrender.com

# Email (Console backend for staging)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@numerai.app

# OpenAI (for Sprint 2)
OPENAI_API_KEY=

# Firebase (for Sprint 2)
FIREBASE_PROJECT_ID=
FIREBASE_CREDENTIALS_PATH=